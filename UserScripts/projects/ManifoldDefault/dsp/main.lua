-- First-pass project DSP entry.
-- This is a placeholder scaffold so the project layout exists now.
-- Phase 4/5 will replace this with the real project-backed looper DSP entry.

function buildPlugin(ctx)
  return {}
end
