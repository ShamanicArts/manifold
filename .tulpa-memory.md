# Tulpa Memory — Manifold Plugin Project

## 2026-04-09

**User's Work (5 commits):**
- Built comprehensive performance attribution system for Manifold Filter plugin
- Added FrameTimings with stage-based memory tracking (Lua init, bindings, script load, DSP, UI open, UI idle)
- Created perf_overlay.lua/ui showing Tot/Plug/UI memory deltas with PSS/Private Dirty/Heap breakdown
- Integrated `/proc/self/smaps_rollup` and `mallinfo2()` for Linux memory metrics
- Settings panel with OSC/OSCQuery toggles
- Reports: 260409_filter_plugin_cost_findings.md documenting ~42MB plugin-attributable, ~70MB UI-attributable memory

**Key Finding:** The "missing memory" is likely JUCE/OpenGL framework allocations, not plugin-owned structures.

**My Work:**
- Research: External sources confirm JUCE OpenGL context adds ~50-60MB overhead — aligns with user's 70MB UI delta
- Document: research/2026-04-09-audio-plugin-memory-profiling.md with findings and recommendations
- Prototype: prototypes/2026-04-09-malloc-trim-endpoint.html showing malloc_trim diagnostic endpoint approach
- Key recommendations: render-pause experiment, malloc_trim endpoint, allocator retention tracking

**Understanding:** User is deep in performance engineering — they value precise attribution over raw metrics. Their stage-based approach is correct methodology. Future work should focus on framework-level diagnostics, not chasing non-existent plugin leaks.

**Position:** Anonymous commit @ lvtxtpswtlrl (user's latest)

---

## 2026-04-10

**User's Work (4 commits):**
- Built comprehensive **plugin export infrastructure** — major pivot from performance to product platform
- Created shared scaffold system (`export_plugin_scaffold.lua`) for exporting any rack module
- Added **FX export** (21 effect types) with correct parameter recall on type switching
- Fixed tricky bug: effect type switching now syncs public/internal param state correctly
- Generator system in `tools/generate_export_manifest.py` with support for filter_single, eq8_single, fx_single
- CMake shared macros for multi-format builds (Standalone, VST3)
- Deployment scripts and export-specific UI components (square styling for exports)
- Renamed/refactored Standalone_Filter → Standalone_Eq and Standalone_FX with proper manifest.spec.json files

**Key Insight:** User is building a **plugin factory** — turning Manifold synth modules into standalone commercial plugins (Filter, EQ, FX). This is a product platform strategy.

**My Work:**
- Research: `research/2026-04-10-plugin-format-comparison-validation.md` — VST3/CLAP/AU/AUv3 comparison, validation strategies, risks specific to Lua-embedded plugins
- Prototype: `prototypes/2026-04-10-plugin-validation-dashboard.html` — web-based validation dashboard for automated testing
- Key findings:
  - VST3 is right primary choice (universal support)
  - CLAP offers faster preset loading, cleaner threading — worth evaluation
  - AU needed for macOS Logic/GarageBand
  - Critical risk: OSC port conflicts with multiple instances
  - Critical risk: Lua runtime isolation across instances
  - State versioning needed for preset compatibility

**Understanding:** User values infrastructure that scales. The shared scaffold approach (one system for all exports) matches their engineering style. They're solving real integration problems (FX parameter recall) not just plumbing. Next work should support the plugin factory vision: validation automation, format expansion, distribution packaging.

**Position:** Anonymous commit @ oqxtzkywsywq (user's latest)

---

## 2026-04-12

**User's Work (0 commits):**
- No new activity since 2026-04-10
- Last commit: uksqssnzvytp (same as previous check)

**State Analysis:**
- FX export already uses shared `export_plugin_shell.lua` builder pattern
- Filter and EQ exports still use legacy manual copy/paste wrappers
- Follow-ups document (2026-04-10) identified this as technical debt

**My Work:**
- **Script:** `scripts/deploy_all_exports.sh` — automated VST3 deployment helper
  - Deploys Filter, EQ, FX to `~/.vst3/` with verification
  - Handles build-type selection (RelWithDebInfo/Release)
  - Reports deployed/skipped/failed status
  
- **Proposal:** `suggestions/2026-04-12-filter-eq-shared-shell-migration.md`
  - Migration guide for Filter/EQ to use shared shell (like FX)
  - Addresses root cause of wrapper copy/paste bugs
  - Includes verification checklist and risk mitigations

**Key Observation:** The FX export shows the "right" pattern — declarative builder with shared shell. Filter/EQ should migrate to match. This would eliminate the class of bugs documented in follow-ups (component ID drift, overlay mounting issues, etc.)

**Understanding:** User prioritizes working systems over perfect ones. The FX export works well with the shared shell — Filter/EQ work too but carry maintenance burden. A migration proposal gives them a roadmap when they return to this area. The deployment script addresses the "Build is not deploy" friction noted in follow-ups.

**Position:** Anonymous commit @ uksqssnzvytp (user's latest, no change)

---
