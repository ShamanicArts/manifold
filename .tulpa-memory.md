# Manifold Tulpa Memory

## 2026-04-09

### User's Work Analysis
User's last commit was 2026-04-08 09:08 (qsypzkrk). Working copy is clean - no new commits in last ~12 hours.

**Recent Work Context:**
- VST Export System implementation (2026-04-08)
- TODOs identified in `BehaviorCoreProcessor.cpp` around state subscription management:
  - `subscribeToPath` - Subscription management (line 3122)
  - `unsubscribeFromPath` - Unsubscription (line 3128)  
  - `clearSubscriptions` - Subscription clearing (line 3133)
  - `processPendingChanges` - Pending change processing (line 3137)
  - `serializeStateToJson` - JSON serialization for OSCQuery (line 3064)
  - `getStateSchema` - State schema for OSCQuery (line 3070)

### My Work Today

1. **Research Document**: Created `research/2026-04-09-state-subscription-patterns.md`
   - Analyzed lock-free state subscription patterns for real-time audio
   - Three pattern options: Lock-Free Path-Subscriber Map, Hierarchical Observer, Simple Ring Buffer
   - Memory ordering strategy matching user's existing `SPSCQueuePtr` patterns
   - Integration approach with existing OSCQuery WebSocket LISTEN/IGNORE protocol
   - Phased implementation recommendations (Simple → Generic → Hierarchical)

2. **Interactive Prototype**: Created `prototypes/2026-04-09-state-subscription-worklet.html`
   - Live simulation of lock-free SPSC queue for state notifications
   - Visualizes audio thread (producer) and message thread (consumer) interaction
   - Demonstrates memory ordering semantics with release-acquire patterns
   - Shows integration code for `BehaviorCoreProcessor`
   - Metrics visualization for queue depth, processed notifications

### Key Findings

The TODOs in BehaviorCoreProcessor represent a needed abstraction layer between:
- **Audio thread**: Parameter changes, state updates (needs lock-free)
- **Message thread**: WebSocket broadcasts, UI updates, VST automation (can use locks)

Current OSCQuery has hardcoded state caching per WebSocket client. The new system should generalize this to support arbitrary path subscriptions from:
- VST host automation (via `parameterChanged()`)
- WebSocket OSCQuery clients (LISTEN commands)
- UI components (data binding)
- Lua scripts (observation)

### Observed Preferences (Updated)

- **Lock-free audio thread**: User consistently uses `memory_order_acquire/release` patterns
- **Pre-allocation**: `SPSCQueuePtr` uses fixed-size arrays, no malloc in real-time
- **Separation of concerns**: Audio thread queues, message thread dispatches
- **OSCQuery integration**: WebSocket LISTEN/IGNORE already implemented - new system should bridge to it

### Next Time Consider

- SIMD optimization for DSP (recurring theme)
- Preset management system for exported VSTs
- Automated VST validation/testing approach
- CLAP plugin format (modern VST alternative)
- Sidechain I/O implementation patterns

---

## 2026-04-08

### User's Work Analysis
User made significant commits on 2026-04-07/08 implementing **VST Export System** for Manifold modules:

**Files Changed:**
- `BehaviorCoreProcessor.cpp/h` - Core VST export infrastructure (+720 lines, +70 lines)
- `BehaviorCoreEditor.cpp/h` - Editor integration
- `UserScripts/projects/Standalone_Filter/` - New standalone filter export target
  - `manifold.project.json5` - Plugin manifest with parameter mapping
  - `ui/behaviors/main.lua` - Filter behavior implementation
  - `ui/main.ui.lua` - UI layout
- `UserScripts/projects/Main/ui/behaviors/filter.lua` - Filter component updates

**Key Infrastructure Added:**
1. `ExportPluginConfig` - Configuration from project manifest
2. `ExportParamAlias` - Maps internal state paths to VST host parameters
3. `AudioProcessorValueTreeState` integration for host parameter management
4. Dual-path state system (public/internal) with bidirectional sync
5. View mode support (compact: 236×220, split: 472×220)
6. OSC port scanning and availability checking
7. Port availability checking (`isUdpPortAvailable`, `isTcpPortAvailable`)

**Pattern Observed:**
```cpp
// Thread-safe parameter access
std::atomic<float>* rawHostValue;  // Audio thread reads
// UI thread writes via AudioProcessorValueTreeState
```

### Inferred Direction
User is building infrastructure for the TODO item:  
> "Build wrapper project for exporting standalone modules as VSTs"

This is a foundational feature allowing users to export individual Manifold modules (starting with filters) as standalone VST plugins with:
- Full DAW automation support
- Multiple view modes
- Remote OSC control
- Parameter aliasing/mapping

Next likely steps:
1. More module types (compressor, reverb, etc.)
2. Preset management within exported plugins
3. MIDI learn for exported parameters
4. Sidechain input support
5. Automated testing/validation

### My Work Today

1. **Research Document**: Created `research/2026-04-08-vst-export-architecture.md`
   - Analyzed user's implementation patterns
   - Documented manifest schema
   - Researched VST3 best practices
   - Identified potential improvements (parameter groups, program change, latency reporting)

2. **Interactive Prototype**: Created `prototypes/2026-04-08-vst-parameter-worklet.html`
   - Live Web Audio demonstration of parameter mapping
   - View mode switching (compact/split)
   - Logarithmic frequency mapping with skew
   - Bidirectional sync simulation
   - OSC control simulation
   - Real-time filter visualization

### Observed Preferences

- **Systematic architecture**: User builds comprehensive infrastructure before adding features
- **Thread safety first**: Lock-free atomic patterns for audio thread
- **Clean separation**: Export configuration isolated from core DSP
- **JJ workflow**: Anonymous commits, no forced messages
- **Manifest-driven**: JSON5 configuration for project metadata
- **Dual-path design**: Public paths for host, internal paths for Manifold state

### Adjusted Understanding

The VST export system reveals the user's broader vision: **Manifold as a platform for creating modular audio plugins**. Each module can be:
- Used within the full Manifold environment
- Exported as standalone VST
- Controlled via OSC
- Automated by DAWs

This is sophisticated product architecture bridging the gap between modular environments (like Max/MSP, Reaktor) and traditional plugin formats.

### Next Time Consider

- SIMD optimization for DSP (mentioned in previous analysis)
- Preset management system research
- Sidechain I/O implementation patterns
- Automated VST validation/testing frameworks
- CLAP plugin format support (modern alternative to VST3)
