# Tulpa Memory — Manifold Plugin Project

## 2026-04-09

**User's Work (5 commits):**
- Built comprehensive performance attribution system for Manifold Filter plugin
- Added FrameTimings with stage-based memory tracking (Lua init, bindings, script load, DSP, UI open, UI idle)
- Created perf_overlay.lua/ui showing Tot/Plug/UI memory deltas with PSS/Private Dirty/Heap breakdown
- Integrated `/proc/self/smaps_rollup` and `mallinfo2()` for Linux memory metrics
- Settings panel with OSC/OSCQuery toggles
- Reports: 260409_filter_plugin_cost_findings.md documenting ~42MB plugin-attributable, ~70MB UI-attributable memory

**Key Finding:** The "missing memory" is likely JUCE/OpenGL framework allocations, not plugin-owned structures.

**My Work:**
- Research: External sources confirm JUCE OpenGL context adds ~50-60MB overhead — aligns with user's 70MB UI delta
- Document: research/2026-04-09-audio-plugin-memory-profiling.md with findings and recommendations
- Prototype: prototypes/2026-04-09-malloc-trim-endpoint.html showing malloc_trim diagnostic endpoint approach
- Key recommendations: render-pause experiment, malloc_trim endpoint, allocator retention tracking

**Understanding:** User is deep in performance engineering — they value precise attribution over raw metrics. Their stage-based approach is correct methodology. Future work should focus on framework-level diagnostics, not chasing non-existent plugin leaks.

**Position:** Anonymous commit @ lvtxtpswtlrl (user's latest)

---

## 2026-04-10

**User's Work (4 commits):**
- Built comprehensive **plugin export infrastructure** — major pivot from performance to product platform
- Created shared scaffold system (`export_plugin_scaffold.lua`) for exporting any rack module
- Added **FX export** (21 effect types) with correct parameter recall on type switching
- Fixed tricky bug: effect type switching now syncs public/internal param state correctly
- Generator system in `tools/generate_export_manifest.py` with support for filter_single, eq8_single, fx_single
- CMake shared macros for multi-format builds (Standalone, VST3)
- Deployment scripts and export-specific UI components (square styling for exports)
- Renamed/refactored Standalone_Filter → Standalone_Eq and Standalone_FX with proper manifest.spec.json files

**Key Insight:** User is building a **plugin factory** — turning Manifold synth modules into standalone commercial plugins (Filter, EQ, FX). This is a product platform strategy.

**My Work:**
- Research: `research/2026-04-10-plugin-format-comparison-validation.md` — VST3/CLAP/AU/AUv3 comparison, validation strategies, risks specific to Lua-embedded plugins
- Prototype: `prototypes/2026-04-10-plugin-validation-dashboard.html` — web-based validation dashboard for automated testing
- Key findings:
  - VST3 is right primary choice (universal support)
  - CLAP offers faster preset loading, cleaner threading — worth evaluation
  - AU needed for macOS Logic/GarageBand
  - Critical risk: OSC port conflicts with multiple instances
  - Critical risk: Lua runtime isolation across instances
  - State versioning needed for preset compatibility

**Understanding:** User values infrastructure that scales. The shared scaffold approach (one system for all exports) matches their engineering style. They're solving real integration problems (FX parameter recall) not just plumbing. Next work should support the plugin factory vision: validation automation, format expansion, distribution packaging.

**Position:** Anonymous commit @ oqxtzkywsywq (user's latest)

---

## 2026-04-12

**User's Work (0 commits):**
- No new activity since 2026-04-10
- Last commit: uksqssnzvytp (same as previous check)

**State Analysis:**
- FX export already uses shared `export_plugin_shell.lua` builder pattern
- Filter and EQ exports still use legacy manual copy/paste wrappers
- Follow-ups document (2026-04-10) identified this as technical debt

**My Work:**
- **Script:** `scripts/deploy_all_exports.sh` — automated VST3 deployment helper
  - Deploys Filter, EQ, FX to `~/.vst3/` with verification
  - Handles build-type selection (RelWithDebInfo/Release)
  - Reports deployed/skipped/failed status
  
- **Proposal:** `suggestions/2026-04-12-filter-eq-shared-shell-migration.md`
  - Migration guide for Filter/EQ to use shared shell (like FX)
  - Addresses root cause of wrapper copy/paste bugs
  - Includes verification checklist and risk mitigations

- **Research:** `research/2026-04-12-midi-effect-validation-strategies.md`
  - Validation patterns for MIDI effect exports (Arp, ScaleQuantizer, etc.)
  - Golden file testing, property-based testing, stress testing approaches
  - Host compatibility matrix (Live, Bitwig, Reaper, Logic, FL)

**Key Observation:** The FX export shows the "right" pattern — declarative builder with shared shell. Filter/EQ should migrate to match. This would eliminate the class of bugs documented in follow-ups (component ID drift, overlay mounting issues, etc.)

**Understanding:** User prioritizes working systems over perfect ones. The FX export works well with the shared shell — Filter/EQ work too but carry maintenance burden. A migration proposal gives them a roadmap when they return to this area. The deployment script addresses the "Build is not deploy" friction noted in follow-ups.

**Position:** Anonymous commit @ uksqssnzvytp (user's latest, no change)

---

## 2026-04-13

**User's Work (1 commit):**
- Extended MIDI effect scaffold with **voice state data bridge**
- Added `publishScalarTable()` and `publishVoiceEntries()` functions to `export_midi_effect_scaffold.lua`
- Extended `dynamic_module_ui.lua` to read back voice state via OSC
- New voice fields exposed: `voiceIndex`, `inputNote`, `outputNote`, `note`, `inputAmp`, `outputAmp`, `passes`
- 64 insertions across 2 files — focused, surgical change

**Direction Analysis:**
- User is building **real-time voice visualization** for MIDI effects
- The data flow: scaffold publishes voice state → OSC → UI reads and displays
- This enables visual feedback for polyphonic MIDI effects (Arp, ScaleQuantizer, etc.)
- Pattern matches the plugin factory approach: shared infrastructure for all MIDI effects

**My Work:**
- **Research:** `research/2026-04-13-voice-state-visualization-patterns.md`
  - 4 visualization patterns: Voice Grid, Piano Roll, I/O Comparison, Timeline
  - Design considerations for update rate, effect-type semantics
  - Implementation sketch for Manifold's Lua UI system
  
- **Prototype:** `prototypes/2026-04-13-voice-state-visualizer.html`
  - Interactive web demo showing all 4 visualization patterns
  - Simulates Arpeggiator, ScaleQuantizer, NoteFilter scenarios
  - Displays mock OSC data stream matching scaffold output
  - Voice stealing visualization, real-time updates

**Key Insight:** User is solving the "black box" problem of MIDI effects. Without visualization, users can't see *why* an arpeggiator made certain choices. The voice state bridge enables educational/debugging UI that differentiates Manifold exports from competitors.

**Understanding:** This is a UX differentiator, not just plumbing. The user's pattern suggests: build infrastructure → enable visualization → polish UX. Future work should anticipate what UI they'll need once the data bridge is complete.

**Position:** Anonymous commit @ vqqztnqmvkmn (user's latest)

---

## 2026-04-14

**User's Work (6 commits since last check):**
- `4aa09174` (2026-04-13 21:03)
- `51a3adc3` (2026-04-13 20:06)
- `ce313338` (2026-04-13 18:27)
- `e0afa015` (2026-04-13 18:12)
- `be49acde` (2026-04-13 16:42)
- `74aa8f57` (2026-04-13 12:13)

**Files changed:** 17 files, +3403/-198 lines
- `export_plugin_shell.lua` (+211): Remote discovery system with UDP OSC heartbeat register/unregister, port config, advertising state
- `export_settings_panel.lua/ui.lua` (+139): Settings UI for exported plugins with remote discovery status display
- `dynamic_module_ui.lua` (+37): Param base resolution improvements for instance module IDs
- `export_shell.lua/behaviors` (+82): Export shell behavior wiring
- `web/remote-control/app.ts` (+959/-198): Massive expansion of web remote — custom surface builder with drag-drop, inspector panel, device tree sidebar, canvas editing, layout mirroring, global surface persistence
- `web/remote-control/vite.config.ts` (+137): Vite dev server now hosts a UDP OSC discovery socket, proxies `/__oscq/targets`, `/__oscq/http`, `/__oscq/command`
- `web/remote-control/index.html/style.css/dist` (+566): New app shell UI with tabbed workspace (Generic / Layout / Custom Surface)
- `agent-docs/oscquery-centralization.md` (+1500): Comprehensive architectural documentation for OSCQuery centralization
- `Standalone_Arp/web-remote.layout.json` (+2): Layout update

**Direction Analysis:**
The user has executed a **major platform expansion** in a single day. They are no longer just exporting plugins — they are building a **complete remote control ecosystem** around those exports. The web remote has transformed from a simple parameter list into a professional control surface builder with discovery, layout mirroring, and custom drag-and-drop surfaces. The Vite proxy's UDP discovery socket is particularly sophisticated: exported plugins broadcast their OSCQuery ports via UDP OSC, the dev server listens, and the web app discovers them automatically.

**Key Pattern:** Infrastructure first, then UX. The user builds the data flow (UDP discovery → OSCQuery proxy → web UI), then layers the interface on top. This matches their previous voice-state-bridge work.

**My Work:**
- **Research:** `research/2026-04-14-oscquery-visualization-patterns.md`
  - Analyzed 4 streaming strategies for real-time visualization over OSCQuery (bundle polling, WebSocket, SSE, OSC UDP)
  - Recommended SSE as the "sweet spot" for scopes in this architecture
  - Documented Float32 Base64 encoding, decimation rules, frame dropping, and widget-specific patterns
  - Included integration notes for adding `widgetType: "scope"` to the custom surface builder

- **Prototype:** `prototypes/2026-04-14-remote-surface-scope-widget.html`
  - Self-contained HTML demo of oscilloscope + spectrum analyzer widgets
  - Canvas-based rendering with `devicePixelRatio` scaling for crisp Retina output
  - Simulated signal generator (sine, saw, noise, chords)
  - FFT spectrum analyzer with smoothed bar animation
  - Code snippet showing exactly how to integrate into `buildControl()` in `app.ts`

**Understanding:** The user's web remote is rapidly becoming a product in its own right. The next logical frontier is **real-time visualization widgets** on the custom surface (scopes for filters, spectra for EQs, meters for dynamics). They have the data infrastructure (OSCQuery endpoints, discovery, proxy). What they need next are the rendering patterns and widget types. Future proactive work should anticipate:
1. Meter bundle endpoints in `export_plugin_shell.lua`
2. SSE streaming support in the Vite proxy
3. Additional custom widgets (step sequencer grids, multi-touch chord pads, envelope visualizers)

**Position:** Anonymous commit @ yxoltnkwltrmustvzswkupwrsqpytlnq (user's latest) → new empty tulpa commit @ wxwtwnxp

---
